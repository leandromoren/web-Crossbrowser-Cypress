{
  "devDependencies": {
    "@4tw/cypress-drag-drop": "^2.2.5",
    "cypress": "^13.3.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "abrir:cypress": "cypress open"
  }
}
